Start a local HTTP server on port 8000 in the background for the current project.

Run this bash command in the background:
```
python3 -m http.server 8000
```

Tell the user the server is running at http://localhost:8000
